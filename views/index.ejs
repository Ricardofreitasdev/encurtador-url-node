<!DOCTYPE html>
<html>
  <head>
    <title>Encurtador de url</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Georama:wght@300;500&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <section>
      <h1><%= dados.title %></h1>  
  
      <% if (dados.resultado) { %>
        <div class="url__encurtada">
          <input id="url" value="<%= dados.url %>/<%= dados.resultado.code %>"></input>
          <i id="btn">
            <img src="/images/copy.png" alt="copiar">
          </i>
        </div>
        <p id="msg"></p>
      <% } else { %>
        <form action="/new" method="post">
          <input class="form__input" type="url" placeholder="cole seu link aqui" name="url" />
          <input class="form__button" type="submit" value="encurtar" />
        </form>
      <% }  %>

    </section>
    <span>Developed by <a target="_blank" href="https://github.com/Ricardofreitasdev">Ricardo Freitas</a></span>
    <script src="/javascripts/script.js"></script>
  </body>
</html>
